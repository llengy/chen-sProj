const state = {
  mycart: []
}
const getters = {}
const mutations = {
  UPDATE_CART(state, val) {
    state.mycart = val
  }
}
const actions = {}
export default {
  state,
  getters,
  mutations,
  actions
}